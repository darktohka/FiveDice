<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".gui.activity.InfoActivity">

    <ImageView
        android:id="@+id/imgInfoFiveDice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@android:drawable/ic_delete" />

    <EditText
        android:id="@+id/txtvInfoOut"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:ems="10"
        android:inputType="textMultiLine"
        android:maxLines="@integer/iCustomerNameLine"
        android:minLines="@integer/iCustomerNameLine"
        android:text="@string/strInfoOut"
        android:textColor="@color/black_color"
        android:textSize="20sp"
        android:layout_marginLeft="@dimen/iWidth"
        android:layout_marginRight="@dimen/iWidth"
        />

</LinearLayout>